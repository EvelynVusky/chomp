/* bin operations */

void main() {
    nibble n = {{0101}};
    bit b = {{1}};
    word w = {{1100101010101101}};
    byte b  = n >< b;
    w = w >> 2;
    nibble b = n & n;
    nibble c = (n & n) | ~ n;
    word x = w ^ w;
    nibble n = ~c;
}
