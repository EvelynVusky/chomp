/* Statements */

void main() {
    /* missings paren */
    while (true {}

    /* wrong paren */
    int i;
    for {i = 0; i < 5; i = i + 1} {
        i = i + 2;
    }

    /* missing semi in for */
    for (i =  0; i < 5 i = i + 1) {
        print(i);
    }

    /* wrong statements in for */
    for (i = 0; i < 5; i = i + 1) {
        a + b
    }

    /* misspelled for */
    fr (i = 0; i < 5; i = i + 1) {}

    /* missing statement in while */
    int a = 5;
    while () {
        a = a + 1;
    }

    /* while without paren */
    while {
        a = 5;
    }

    /* wrong statement in while */
    while (a < 10) {
        a = 5 + 1
    }

    /* incorrect prints */
    print ({1, 2, 3});
    print ([1, 2, 3]);;
    println a;
}