int main() {
    char first_initial = 'a';
    char last_initial = 'd';
    int age = 23;
    int grade = 14;
    int credits = 15;
    
    /* around 14,000 undergrad students at Tufts */
    print(first_initial);
    print(last_initial);
    print(age);
    print(grade);
    print(credits);
    
    return 0;
}